# Public site URL — used for canonical tags, Open Graph, sitemap and JSON-LD.
# Set this to your production domain in your host's env settings.
# Falls back to https://spanex.ca if unset.
NEXT_PUBLIC_SITE_URL=https://spanex.ca
